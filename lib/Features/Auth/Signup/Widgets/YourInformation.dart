import 'package:flutter/material.dart';

class YourInformation extends StatelessWidget {
  const YourInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('YourInformation'),
    );
  }
}
