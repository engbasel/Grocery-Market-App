import 'package:flutter/material.dart';

class Enterthepassword extends StatelessWidget {
  const Enterthepassword({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Enterthepassword'),
    );
  }
}
